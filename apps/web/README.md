# @ig-board/web

Next.js 14 (App Router) stub for Boardroom, configured for **static export**
(`output: 'export'`). Placeholder for the board scorecard UI delivered in later
missions.

```bash
npm install
npm run dev --workspace apps/web      # local dev server
npm run build --workspace apps/web    # static export -> apps/web/out
```
