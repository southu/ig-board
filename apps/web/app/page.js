export default function HomePage() {
  return (
    <main style={{ fontFamily: 'system-ui, sans-serif', padding: '3rem', maxWidth: 640 }}>
      <h1>Boardroom</h1>
      <p>Private governance BI for The Image Group.</p>
      <p style={{ color: '#666' }}>
        Foundation shell — the board scorecard, KPI drilldowns, memos and
        analyses arrive in later missions. The API is deployed separately.
      </p>
    </main>
  );
}
