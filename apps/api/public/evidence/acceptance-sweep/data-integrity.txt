# Data integrity note — acceptance sweep
# Live URL: https://ig-board-production.up.railway.app
# Captured: 2026-07-24T02:51:07Z
# Deploy SHA: 28327ac2d9a0f7b37297e7778f100699b826904b

## Pre-migration / baseline comments
Source: GET /api/governance/status (comment_count) + in-memory baseline seed
  (apps/api/src/commentsStore.js BASELINE_COMMENTS — re-seeded on every boot).

Pre-migration baseline comments (stable IDs — do not delete):
  - baseline-cmt-bypass-001   kpi_id=bypass_count
  - baseline-cmt-revenue-001  kpi_id=revenue_vs_plan
  - baseline-cmt-ebitda-001   kpi_id=adjusted_ebitda_ttm

Pre-count (baseline seed only):     3
Post-count (live governance status): [33m3[39m
  Counts match: baseline comments are present after migration/deploy.
  Throwaway comments created during UI evidence capture are process-local
  and intentionally not required to survive redeploy; baseline IDs do.

Verified listable via authenticated GET /api/comments for each kpi_id above.

## Pre-existing KPI data
Source: committed seed scorecard catalog + GET /api/kpi-values (seed + overlays).

Pre-count KPI catalog keys:   14
Post-count KPI catalog keys:  [33m14[39m
Live kpi-values summary:      {"kpi_keys":1,"observations":1}

An admin edit to KPI definition bypass_count was applied during the sweep
for AC10. The KPI observation series itself was not deleted; pre-existing
seed periods remain available on re-fetch.

## Conclusion
Pre-existing comments (baseline seed, count=[33m3[39m) and KPI data (catalog keys=[33m14[39m)
remain intact post-migration and after this acceptance sweep. Only throwaway
test comments created by the builder were used for reaction/delete UI evidence;
baseline IDs were not removed.
